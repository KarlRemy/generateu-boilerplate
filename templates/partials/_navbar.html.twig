<nav class="bg-white border-b border-gray-200">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex h-16 justify-between items-center">
            <div class="flex items-center gap-8">
                <a href="{{ path('app_home') }}" class="text-xl font-bold text-gray-900 hover:text-indigo-600 transition-colors">
                    ⚡ Generateu
                </a>
            </div>

            <div class="flex items-center gap-4">
                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('app_admin_dashboard') }}" class="text-sm text-gray-600 hover:text-gray-900">Admin</a>
                    {% endif %}
                    <span class="text-sm text-gray-500">{{ app.user.email }}</span>
                    <a href="{{ path('app_logout') }}" class="inline-flex items-center rounded-md bg-gray-100 px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-200 transition-colors">
                        Deconnexion
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="text-sm font-medium text-gray-600 hover:text-gray-900">Connexion</a>
                    <a href="{{ path('app_register') }}" class="inline-flex items-center rounded-md bg-indigo-600 px-3 py-2 text-sm font-medium text-white hover:bg-indigo-500 transition-colors">
                        Inscription
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
